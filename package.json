{
  "name": "astro-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "rm -rf dist && astro build && netlify deploy --prod",
    "host": "npm run dev -- --host"
  },
  "dependencies": {
    "@astrojs/netlify": "^6.1.0",
    "astro": "^5.2.5",
    "lucide": "^0.475.0"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-astro": "^0.14.1"
  }
}
